import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Blinks")
                .font(.largeTitle)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
